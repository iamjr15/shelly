---
"shellykit": patch
---

Keep Rust binaries, npm packages, and release tags on one version. This makes `shelly --version` report the installed release correctly and blocks future mismatched publishes.
