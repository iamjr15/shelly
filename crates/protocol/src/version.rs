/// Current protocol contract version.
pub const CONTRACT_VERSION: u32 = 5;

/// Oldest contract version accepted for already-paired remote sessions during
/// the coordinated v4-to-v5 migration window.
pub const MIN_PAIRED_CONTRACT_VERSION: u32 = 4;
